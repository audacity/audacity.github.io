---
title: "Cursor to Track End"
section: "Header"
sectionOrder: 30
order: 36.4
description: Select from the playback position to the end of the selected tracks' audio.
draft: false
---
import Shortcut from "../../../../../../components/manual/Shortcut.jsx";

Selects from the playback position to the end of the audio on the selected
tracks. If no track content is selected, it falls back to selecting from
the track start to the cursor instead — the same behaviour as
[Track start to cursor](/manual/manual-index/header/select/region/track-start-to-cursor).

## Where to find it

**Select → Region → Cursor to track end**, or press
<Shortcut client:load keys="shift+k" />.
