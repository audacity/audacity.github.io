---
title: "Edit"
section: "Header"
sectionOrder: 30
order: 15
description: The commands that change audio you have already recorded or imported.
draft: false
---

The Edit menu collects the commands that change what is already in your
project: undoing and redoing edits, the clipboard commands (Cut, Copy,
Paste), deleting and duplicating audio, and silencing a selection.

Two submenus group the more specific commands. **Clip** holds everything
that works on clips — renaming, trimming, splitting, joining and grouping.
**Label** holds the commands for adding and managing labels.

The menu also opens two dialogs: the metadata editor, for the tags written
into exported files, and Preferences, where Audacity's settings live.
