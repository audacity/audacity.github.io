---
title: "Rename Label"
section: "Header"
sectionOrder: 30
order: 31.4
description: Edit the text of the selected label.
draft: false
---
import Shortcut from "../../../../../../components/manual/Shortcut.jsx";

Opens the selected label's text for editing, directly on the label. Type
the new text and confirm to apply it.

## Where to find it

**Edit → Label → Rename label**, or press <Shortcut client:load keys="f2" />.

## Notes

The command needs exactly one item selected — it is disabled when nothing,
or more than one label, is selected. The same shortcut renames a selected
clip; see [Rename Clip](/manual/manual-index/header/edit/clip/rename-clip).
