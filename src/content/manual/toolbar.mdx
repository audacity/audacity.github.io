---
title: "Tools toolbar"
description: "The toolbar you work in: transport, editing tools, zoom, the playhead position, snapping and the record level — every control, explained."
section: "Toolbars"
sectionOrder: 20
order: 2
---

import ManualToolbarDemo from "../../components/manual/interactive/ManualToolbarDemo.jsx";
import UIExample from "../../components/manual/UIExample/UIExample";
import UIShot from "../../components/manual/UIShot.astro";

<ManualToolbarDemo client:load />

# Tools toolbar

The [three toolbars](/manual/toolbars) split the window between
them; this one is where the work happens — playback and recording, the
editing tools, zoom, the playhead position, snapping and the record level.
It is also the only toolbar you can move.

<UIShot
  name="app/ToolbarTools"
  alt="The Tools toolbar: transport controls, editing tools, zoom, the playhead timecode, snapping and the record level button"
/>

## Moving the Tools toolbar

Grab the Transport Toolbar's drag handle and drag it:

- Drop it near the top of the window to dock it at the top.
- Drop it near the bottom of the window to dock it at the bottom.
- Drop it anywhere else to leave it floating at that position.

The Project Toolbar and Selection Toolbar stay fixed in place.

## Navigating toolbars with the keyboard

Once a toolbar has focus, arrow keys move between its buttons and controls. See Accessibility and Keyboard Navigation for the full keyboard navigation model, including an alternative mode for screen reader users.

## The controls, by group

- [Transport](/manual/toolbar/transport) — Play, stop, record, the skips and the loop toggle.
- [Tools](/manual/toolbar/tools) — The editing tools — toggles that change what clicking does, and buttons that act on the selection.
- [Zoom tools](/manual/toolbar/zoom-tools) — Getting closer to the waveform and back out again.
- [Selection](/manual/toolbar/selection) — The playhead position, as a number you can read and edit.
- [Music](/manual/toolbar/music) — Tempo and time signature, shown in the Music workspace.
- [Snapping](/manual/toolbar/snapping) — Snap edits, clips and selections to a grid.
- [Metering](/manual/toolbar/metering) — The record and playback level meters.
