// Stub. Original Reassurance section was folded into UnderTheHood in
// commit ac501f2; the live /about page (v3) doesn't reference this.
// Kept as an empty component so the legacy /about-v2 route still
// builds without touching the v2 page itself.
function Reassurance() {
  return null;
}

export default Reassurance;
